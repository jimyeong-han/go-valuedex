# 변경 기록

GO ValueDex의 사용자에게 보이는 변경 사항을 버전별로 기록합니다.

버전 정책:

- `1.0`: 최초 공개 버전
- `1.1` 이후: 큰 기능 단위의 마이너 릴리즈
- 긴급 오류 수정이 필요하면 `1.1.1`과 같이 패치 버전 사용

## [1.7] - 2026-09-01

### 추가

- 현재 공격·방어·체력 IV와 강화 레벨을 바꾸면 검증된 Pokémon GO CP 계산식으로 현재 예상 CP를 즉시 표시
- 도감 상세에서 노말 기술과 차지 기술 두 개를 선택해 `현재 개체 저장`으로 보유함에 함께 기록하는 빠른 저장 흐름
- 레이드 공격수 순위를 3마리씩 묶어 `포켓몬 - 포켓몬 - 포켓몬` 형태로 요약하고, 누르면 기존 개별 기술·DPS·TDO·처치시간 정보를 펼치는 추천 트리오 UI
- 보스 난이도에 `다이맥스 (일반 기술 근사)` 프리셋과 Max 지원 종·보유 Max 개체 전용 후보 필터 추가

### 변경

- 보유함 편집의 특별 기술 선택지에서 긴 획득 경로 괄호를 제거하고 `특별 기술`·`상태 전용` 배지만 간결하게 유지
- 다이맥스 프리셋에서는 그림자·Mega·동료 Mega 옵션을 비활성화하고, 내 보유함 분석은 실제 다이맥스·거다이맥스 자격이 저장된 개체만 사용
- 다이맥스 결과의 DPS·TDO·시간·인원 표기를 일반 기술 구간의 레이드식 근사치로 구분하고, 맥스미터·맥스 기술·가드·스피릿과 실제 보스 배율이 제외됨을 화면에 명시
- 추천 트리오에서 같은 도감 원본이나 같은 보유 개체의 일반·Mega 대안을 중복하지 않고, 한 트리오의 Mega를 공식 활성 제한에 맞춰 최대 한 마리로 제한

### 수정

- 원시 포켓몬 객체를 보스로 전달할 때 공격수 IV를 보스 IV로 재사용하거나, 같은 기술 ID를 공유하면 공격수 기술을 미지정 보스 기술로 잘못 재사용해 예상 TDO가 표시되던 계산 입력 충돌 수정
- 그림자/APEX 상태에서 일반 상태로 전환한 뒤 사용할 수 없는 상태 전용 기술이 빠른 저장에 남던 문제를 수정하되, 기존 보유함의 알 수 없는 레거시 기술은 그대로 보존

### 검증

- 이상해씨 `10/10/10 Lv.20 CP 590`, 공격 `15`일 때 `CP 613`, `10/10/10 Lv.20.5 CP 605` 실시간 갱신 회귀 테스트
- 선택한 리자몽 노말·차지 기술 두 개의 IndexedDB 저장·새로고침 유지와 특별 기술 라벨 간소화 브라우저 테스트
- 추천 트리오의 기본 접힘·3마리 이름·순위·펼침 동작, 원본·Mega 중복 제한과 다이맥스 도감/보유함 후보 제한·부분 트리오·390px/320px 오버플로 테스트
- 공격수 IV와 보스 IV의 분리, 공격수와 보스가 같은 기술을 써도 보스 기술 미지정 상태에서는 예상 TDO가 생성되지 않는 계산 회귀 테스트

### 알려진 제한

- 다이맥스 프리셋은 맥스배틀 전체 시뮬레이터가 아닙니다. 현재 데이터로 검증할 수 없는 보스별 HP·배율을 임의 추정하지 않고, 수정 가능한 5성 레이드 초깃값으로 일반 기술 구간만 비교합니다.

## [1.6] - 2026-09-01

### 추가

- 공격·방어·체력 IV 슬라이더에 `0–5–10–15` 구간을 한눈에 볼 수 있는 5·10 구분선 추가
- 각 IV를 `0–15` 숫자로 직접 입력하고 슬라이더와 양방향으로 즉시 동기화하는 입력 방식 추가
- PvP·중립 PvE 추천과 레이드 보스별 최적 조합에 일반 기술머신으로 배울 수 없는 기술이 포함되면 `특별 기술` 배지와 획득 경로 안내 표시
- 화룡점정·시간의포효·공간절단·프리즈볼트·콜드플레어·메테오드라이브·섀도레이·거수참·거수탄을 정확한 폼에 연결하고, 이벤트 포획·운석·합체·폼 체인지처럼 모든 기술머신으로 배울 수 없는 경로를 별도 표시
- 그림자·정화·APEX 전용 기술에는 일반 이벤트 기술과 구분되는 `상태 전용` 배지 표시

### 변경

- 숫자 IV 입력의 빈값·소수·범위 밖 값을 `0–15` 정수로 안전하게 정규화하고 모바일 숫자 키패드와 44px 조작 영역 지원
- 보유함 기술 선택지의 모호한 `ELITE` 문구를 기술별 `특별 기술 (획득 경로 · TM 제한)` 안내로 구체화
- 대단한 기술머신 전용과 모든 기술머신 사용 불가를 구분하고, 이벤트 기간·현재 재획득 가능 여부는 실시간 정보가 아님을 안내

### 검증

- 세 IV 슬라이더의 구분선 값·위치·접근성 제외와 슬라이더↔숫자 입력 동기화·경계 정규화 브라우저 테스트
- 리자몽 블라스트번과 메타그로스 코멧펀치의 폼별 특별 기술 판정, 일반 동명 기술의 오표시 방지 회귀 테스트
- 오리진폼 디아루가의 시간의포효와 레쿠쟈의 화룡점정이 추천·보유함·보스 분석에서 정확한 획득 경로와 함께 유지되는 회귀 테스트
- 보유 개체의 레이드 최적 조합에서 특별 기술·상태 전용 기술 배지와 설명을 검증하는 브라우저 테스트
- 390px에서 숫자 입력과 긴 특별 기술 안내가 가로 스크롤 없이 표시되는지 검증

## [1.5] - 2026-09-01

### 추가

- 일반·메가·원시 보스의 정확한 폼, 레이드 난이도, 기술과 날씨를 선택하는 보스별 PvE 분석 화면
- 전체 도감의 레벨·공격 IV 조건 또는 내 보유함의 실제 폼·IV·레벨·저장 기술을 기준으로 한 공격수 순위
- 복합 타입 상성, 자속, 날씨, 그림자 공격, Mega 폼과 선택한 동료 Mega 타입을 기술별로 반영한 DPS 계산
- 선택한 보스 기술을 반영한 TDO 추정, 이론 처치시간·최소 인원과 노말 기술 공격 IV breakpoint 표시
- 1성·3성·5성·Mega·Mega Legendary 커뮤니티 검증 프리셋과 HP·CPM·제한시간 직접 수정 기능
- 세대 필터에 `1세대 (관동)`부터 `9세대 (팔데아)`까지 지역명 추가
- 보유함의 화풀이·은혜갚기·APEX `+`/`++` 기술을 실제 저장 기술 그대로 계산하고, 지원하지 않는 저장 기술은 임의 대체 없이 제외하는 안전장치

### 변경

- 기존 IV 합계 비율을 `IV 완성도`로 명확히 표시하고, 같은 IV도 포켓몬에 따라 달라지는 `15/15/15 대비 능력치 곱`을 별도 제공
- 중립 PvE 기술 추천과 타입 상성·날씨·보스 방어를 적용한 보스별 결과를 화면과 문서에서 구분
- 보스 DPS는 에너지 상한 100에서 초과분을 버리고 남는 에너지를 다음 사이클로 이월한 장기 평균으로 계산하며, 피격 에너지 등 제외 항목을 결과 바로 옆에 명시
- 보스 분석 조건을 바꾸면 이미 열린 결과를 짧은 지연 후 다시 계산하도록 개선
- Mega 폼에서 일반 폼으로 돌아올 때 자동 난이도와 HP가 5성 프리셋으로 복귀하도록 수정하고, 표준 영문명 `Mega Charizard X` 검색 지원
- 내 보유함 분석 직전에 IndexedDB 초기화를 기다리고 최신 기록을 다시 읽으며, 느린 이전 분석 응답이 새 조건을 덮어쓰지 못하도록 실행 세대 보호 추가
- 숫자 직접 입력을 화면의 최솟값·최댓값과 동일하게 정규화하고, 보스 기술을 하나만 고른 경우 예상 TDO가 계산되지 않음을 표시
- 하품처럼 위력 0인 정상 노말 기술은 최소 피해로 계산하고, PvE 차지 에너지가 없는 기술은 임의 값 대신 분석에서 제외
- 보스 검색 결과가 없을 때 이전 분석 카드가 남는 문제와 분·초 반올 경계 표시를 수정
- 검증된 `main` 커밋과 버전 태그가 일치할 때 변경 기록을 사용해 GitHub Release를 자동 발행

### 검증

- 복합 약점 `×2.56`, 날씨 `×1.2`, 그림자, 기술 타입별 Mega 동료 부스트, 에너지 이월·100 상한, 위력 0 기술, 보스 기술 기반 TDO와 공격 IV breakpoint 단위 테스트 23개
- 같은 `10/10/10` IV가 이상해씨와 뮤츠에서 서로 다른 15/15/15 대비 능력치 곱 비율을 만드는 계산 회귀 테스트
- 레쿠쟈 이중 약점·눈 날씨 DPS, Mega 보스 폼·영문 검색·프리셋 복귀, 입력 경계, 상태 전용 저장 기술, 분석 직전 보유함 갱신 브라우저 테스트
- 390px 대화상자와 최소 지원 폭 320px 내비게이션의 오버플로·44px 터치 영역·글자 크기 회귀 테스트

## [1.4] - 2026-09-01

### 추가

- 로그인 없이 IndexedDB에 폼·상태·IV·레벨·실제 기술·Max 자격·APEX·특훈 계획을 저장하는 내 보유함
- 상세 화면에서 한 번 눌러 현재 개체를 저장하고, 실행 취소하거나 곧바로 상세 정보를 입력하는 빠른 저장 흐름
- 별명·즐겨찾기·태그·메모 편집과 보유함 검색, 상태·태그·즐겨찾기 필터
- 최근 수정·도감 번호·IV 합계·슈퍼/하이퍼/마스터·레이드 PvE·Max·Mega 정렬
- 같은 종과 다른 종을 모두 지원하는 2~4마리 비교표와 용도별 비교 탭
- SHA-256 무결성 검사가 있는 버전형 JSON 백업, RFC 4180 CSV 가져오기·내보내기
- 레코드 v0→v1 자동 마이그레이션, revision 충돌 감지와 검증 후 단일 트랜잭션 가져오기

### 변경

- Max 가능 종에서는 현재 탭과 관계없이 개체별 Max 자격을 입력할 수 있도록 체크 항목을 항상 표시
- 보유함의 파생 점수는 저장하지 않고 현재 도감·PvP 데이터로 다시 계산해 오래된 판정이 남지 않도록 변경
- Shadow·Max·PvP 데이터의 총개수가 같아도 기존 대상이 대량 치환되면 diff guard가 실패하도록 강화
- 배포 artifact에 공개 JSON Schema를 포함하고 주간 PR 게시 실패 시 fallback artifact 보존 경로를 보강

### 안전·복구

- 보유함이 현재 브라우저에만 남고 사이트 데이터 삭제 시 사라질 수 있음을 화면에 상시 안내
- GitHub Pages의 같은 사용자 도메인에 있는 프로젝트끼리 브라우저 저장소 접근 범위를 공유할 수 있음을 알리고 민감한 개인정보를 기록하지 않도록 안내
- 알 수 없는 폼이나 이전 기술은 강제로 삭제하지 않고 경고와 함께 백업에 보존
- 가져오기 파일의 형식·버전·전체 레코드를 먼저 검증하며 오류가 있으면 기존 보유함을 변경하지 않음
- 유효한 최대 보유함도 자체 백업 후 다시 복원할 수 있도록 256MiB 입력 경계와 식별자 길이 상한을 맞추고, CSV 행 수는 파싱 중 조기에 차단
- 같은 revision의 더 늦은 백업을 병합할 때 저장 revision을 반드시 올려 열린 다른 탭의 오래된 편집이 덮어쓰지 못하도록 보강
- 손상되었거나 더 새로운 버전인 저장 원본 한 건 때문에 전체 보유함이 막히지 않도록 정상 기록은 계속 열고, 문제 원본은 별도 복구 JSON으로 보존·내보내기

### 검증

- 보유함 생성·검증·마이그레이션·JSON/CSV 왕복·충돌 정책 단위 테스트 18개
- Python 데이터 계약·diff guard 23개와 계산 모듈 13개 테스트
- 빠른 저장·새로고침 유지·편집/필터·비교 제한·JSON/CSV 복원·잘못된 파일 원자성·390px 오버플로 브라우저 회귀 테스트

## [1.3] - 2026-09-01

### 추가

- `pokemon.json`, `pvp.json` Draft 2020-12 JSON Schema와 `schemaVersion: 1`
- 데이터 생성 시각과 원본별 URL·수집 시각·SHA-256, 레코드별 출처 참조
- 폼·도감 번호·종족값·타입·기술 풀·그림자·Max·PvP 데이터의 비정상 대량 변경을 막는 diff guard
- 정상 문서를 의도적으로 훼손해 검증 실패를 확인하는 데이터 계약·diff guard 회귀 테스트
- 데스크톱과 390px 모바일을 모두 실행하는 Playwright 브라우저 테스트
- 품질검사 성공 후에만 공식 GitHub Pages artifact를 배포하는 워크플로

### 변경

- 주간 데이터 갱신이 `main`에 직접 푸시하지 않고 검증된 전용 브랜치와 검토용 PR만 만들도록 변경
- 타임스탬프만 달라진 스냅샷은 새 PR을 만들지 않도록 실제 데이터·원본 해시 변화를 분리
- Playwright를 `1.62.1`로 고정하고 외부 이미지 네트워크 없이 `/go-valuedex/` 프로젝트 하위 경로를 재현

### 수정

- 진화 계보 또는 `같은 IV로 진화하면`에서 다른 진화 단계로 이동할 때 선택한 리그가 슈퍼리그로 초기화되던 문제 수정
- 진화 이동 후에도 IV·레벨뿐 아니라 리그 탭과 해당 리그 계산 결과를 그대로 유지

### 검증

- 오프라인 JSON Schema·출처 교차참조와 기존 1,188폼/1,607개 PvP 랭킹 검증
- 데이터 계약 9개와 diff guard 12개 회귀 테스트
- 계산 모듈 13개, Playwright 데스크톱 8개·390px 모바일 2개 테스트 통과

## [1.2] - 2026-09-01

### 추가

- 일반·그림자·정화 상태 선택과 상태별 공격·방어·CP·비용 안내
- 원본 개체를 바꾸지 않는 정화 전후 IV·레벨·CP·리그 가치 비교
- 폼별 정화 비용, 화풀이·은혜갚기, APEX 루기아·칠색조의 `+ → ++` 기술 예외
- 금색·은색병뚜껑 목표 IV와 과제 수를 계산하는 대단한 특훈 플래너
- 특훈 전후 PvP·PvE·Max 평가와 제한 리그 CP 초과 경고
- 현재 폼의 전투 활용도를 보여주는 `핵심 실전용 / 조건부 실전용 / 수집·관상 중심` 분류와 진화 후 실전 후보 표시

### 변경

- 도감명을 `가치를 아는 도감`에서 더 직관적인 `Pokémon GO 개체값·실전 도감`으로 변경
- CP·IV 계산을 DOM과 분리된 순수 `mechanics.js` 모듈로 이동
- 실전 분류에서 PvP 리그별 순위·점수, 같은 타입 자속 레이드 화력, Mega 화력과 개체별 Max 자격을 서로 독립적으로 판정
- 그림자 상태에서 Mega·Max와 대단한 특훈을 비활성화하고, 추천 기술의 일반 폼 참고 범위를 명시
- 정화된 개체를 직접 입력할 때 IV `+2`와 레벨 보정을 중복 적용하지 않도록 구분

### 검증

- 이상해씨 `10/10/10 Lv.20 CP 590 → 정화 12/12/12 Lv.25 CP 761` fixture
- 마릴리 `0/15/15 Lv.45.5 CP 1499 → 공격 +1 CP 1512` 리그 제한 경고 fixture
- 그림자 CP·HP 불변, 공격 `×1.2`, 방어 `×5/6`, 정화 IV 상한과 병뚜껑 자격·목표 불변성 단위 테스트 13개
- 데스크톱 및 390px 모바일 Chrome 상태 선택·정화·특훈·실전 분류 회귀 테스트 35개

## [1.1] - 2026-09-01

### 추가

- 1,024개 도감 번호에서 1,188개 평가 폼을 생성하는 폼별 데이터 모델
- 테오키스, 기라티나, 랜드로스, 알로라·가라르·히스이 폼 등의 독립 검색·평가
- 상세 화면의 폼 전환 칩과 `speciesKey` 기반 공유 URL
- 지역폼을 유지하는 진화 계보와 최종 진화 가치 투영

### 변경

- URL, IV 순위 캐시, PvP 메타, 진화 연결의 식별자를 도감 번호에서 `speciesKey`로 전환
- 동일 전투 데이터를 가진 의상·색상·패턴은 진화 계열 전체에서 하나로 합치고, 종족값·타입·기술·진화가 다른 폼과 성별 폼은 분리
- PvPoke 연결 시 도감 번호뿐 아니라 정확한 폼 별칭, 종족값, 순서와 무관한 타입, 기술 목록을 함께 검증
- 다이맥스·거다이맥스 표시는 확인된 기본 폼과 로우키 스트린더·연격 우라오스처럼 공식 지원되는 대체 폼에만 유지
- 진화 비용이 다른 데인차·차데스의 진작/위작 계열을 독립 폼과 올바른 진화 대상으로 보존

### 검증

- 테오키스 4폼, 기라티나 2폼, 랜드로스 2폼, 알로라 꼬렛 진화, 히스이 찌리리공 타입 순서 회귀 검사
- 잘못된 닥트리오 PvP 데이터 연결 방지와 자시안 일반/검왕 타입 분리 검사
- 비비용·플라베베 색상 계열의 과도한 계보 결합 방지와 데인차·차데스 50/400 사탕 진화 회귀 검사
- 데스크톱 및 390px 모바일 Chrome 폼 라우팅·검색·진화 스모크 테스트

## [1.0] - 2026-09-01

### 추가

- 한국어·영문명·도감 번호 검색과 타입·세대·특수 형태 필터
- 공격·방어·체력 IV 및 강화 레벨 슬라이더
- 슈퍼·하이퍼·마스터리그의 4,096개 IV 조합 전수 순위
- PvP 종족 메타와 개체 IV 가치를 분리한 설명
- PvE 중립 사이클 기술 추천과 공격 우선 평가
- 진화 계보와 최종 진화체 가치 투영
- 종별 엘리트 기술, Mega, Dynamax, Gigantamax 안내
- 모바일·태블릿·데스크톱 반응형 UI
- 주간 데이터 갱신 GitHub Actions와 데이터 회귀 검증

### 알려진 제한

- 도감 번호마다 대표 폼 하나만 제공
- 그림자·정화·하이퍼트레이닝 미지원
- PvE 결과는 보스 상성·날씨·실제 breakpoint를 반영하지 않은 중립값
- 개인 보유함과 개체 비교 저장 기능 미지원

[1.0]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.0
[1.1]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.1
[1.2]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.2
[1.3]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.3
[1.4]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.4
[1.5]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.5
[1.6]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.6
[1.7]: https://github.com/jimyeong-han/go-valuedex/releases/tag/v1.7
